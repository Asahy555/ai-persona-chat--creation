# 🚀 Инструкция по настройке локальных ИИ моделей

## 📋 Что вам нужно

Это приложение использует **полностью бесплатные локальные ИИ модели без цензуры**, которые работают в России без VPN.

### 🎯 Рекомендуемая конфигурация:
- **Для текста**: Ollama + llama2-uncensored (локально)
- **Для изображений**: Fooocus (локально)
- **Резерв**: GigaChat (облачно, работает в РФ)

---

## 📥 ШАГИ УСТАНОВКИ

### 1️⃣ Установка Ollama (для ответов персонажей)

**Windows / Mac:**
1. Перейдите на https://ollama.com
2. Скачайте установщик для вашей ОС
3. Установите и запустите

**Linux:**
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

**Запуск и настройка:**
```bash
# Запустите Ollama сервер
ollama serve

# В новом терминале загрузите модель без цензуры
ollama pull llama2-uncensored

# Проверьте, что модель загружена
ollama list
```

**Альтернативные модели (выберите одну):**
```bash
# Для лучшего русского языка и качества
ollama pull dolphin-mistral

# Для самого высокого качества (требует мощный ПК)
ollama pull dolphin-mixtral

# Проверенная стабильная модель
ollama pull wizard-vicuna-uncensored
```

---

### 2️⃣ Установка Fooocus (для генерации изображений)

**Требования:**
- Python 3.10+
- NVIDIA GPU с 6GB+ VRAM (рекомендуется)
- 10GB свободного места на диске

**Установка:**
```bash
# Клонируйте репозиторий
git clone https://github.com/lllyasviel/Fooocus.git
cd Fooocus

# Запустите (первый запуск загрузит модели ~4GB)
python launch.py
```

После запуска Fooocus откроется на `http://localhost:7860`

**Для CPU (без GPU):**
```bash
python launch.py --cpu
```
⚠️ Генерация на CPU будет очень медленной (10-30 минут на изображение)

---

### 3️⃣ (Опционально) Регистрация в GigaChat

GigaChat - это российский ИИ от Сбербанка. Он будет использоваться как резерв, если Ollama недоступна.

**Получение ключей:**
1. Перейдите на https://developers.sber.ru/portal/products/gigachat
2. Зарегистрируйтесь или войдите
3. Создайте новое приложение
4. Скопируйте `CLIENT_ID` и `CLIENT_SECRET`

**Бесплатные лимиты:**
- GigaChat 2 Lite: 900,000 токенов/год (~5,000-10,000 запросов)
- Обновляется каждый год

---

### 4️⃣ Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
# Скопируйте пример
cp .env.example .env.local
```

Отредактируйте `.env.local`:

```env
# Ollama (должна быть запущена)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama2-uncensored

# GigaChat (опционально, для резерва)
GIGACHAT_CLIENT_ID=ваш_client_id
GIGACHAT_CLIENT_SECRET=ваш_client_secret

# Fooocus (должна быть запущена)
FOOOCUS_API_URL=http://localhost:7860
```

---

## ✅ Проверка установки

### Проверьте Ollama:
```bash
# Должен вернуть список моделей
ollama list

# Тест генерации
ollama run llama2-uncensored "Привет, как дела?"
```

### Проверьте Fooocus:
1. Откройте http://localhost:7860 в браузере
2. Введите любой prompt (например, "beautiful landscape")
3. Нажмите "Generate" и дождитесь результата

---

## 🔄 Система Fallback

Приложение автоматически выбирает лучший доступный сервис:

**Для текстовых ответов:**
1. ✅ **Ollama** (локально, без интернета) ← ПРИОРИТЕТ
2. ✅ **GigaChat** (облако, РФ) ← если Ollama недоступна
3. ✅ **OpenRouter** (облако, международный) ← если GigaChat недоступен
4. ⚠️ **Fallback** (заглушки) ← если всё недоступно

**Для генерации изображений:**
1. ✅ **Fooocus** (локально) ← ПРИОРИТЕТ
2. ✅ **AUTOMATIC1111** (локально) ← альтернатива
3. ⚠️ **Fallback** (заглушка SVG) ← если всё недоступно

---

## 💻 Системные требования

### Минимальные (только Ollama + GigaChat):
- CPU: 4 ядра
- RAM: 8GB
- Интернет для GigaChat

### Рекомендуемые (Ollama + Fooocus):
- CPU: 6+ ядер
- RAM: 16GB
- GPU: NVIDIA с 6GB+ VRAM
- Место: 20GB

### Идеальные (всё локально на максимуме):
- CPU: 8+ ядер
- RAM: 32GB
- GPU: NVIDIA RTX 3060+ (12GB VRAM)
- Место: 50GB

---

## 🚨 Решение проблем

### Ollama не запускается:
```bash
# Проверьте, что порт 11434 свободен
netstat -an | grep 11434

# Перезапустите Ollama
killall ollama
ollama serve
```

### Fooocus не генерирует изображения:
- Убедитесь, что у вас установлен Python 3.10+
- Проверьте, что есть NVIDIA GPU с актуальными драйверами
- Для CPU используйте: `python launch.py --cpu`

### "All providers unavailable":
- Убедитесь, что хотя бы один сервис запущен (Ollama или GigaChat настроен)
- Проверьте файл `.env.local`
- Посмотрите логи сервера в консоли

---

## 📚 Дополнительные ресурсы

- **Ollama**: https://ollama.com
- **Список uncensored моделей**: https://ollama.com/search?q=uncensored
- **Fooocus**: https://github.com/lllyasviel/Fooocus
- **GigaChat документация**: https://developers.sber.ru/docs/ru/gigachat
- **AUTOMATIC1111** (альтернатива Fooocus): https://github.com/AUTOMATIC1111/stable-diffusion-webui

---

## 🎉 Готово!

После настройки запустите приложение:

```bash
bun install
bun dev
```

Откройте http://localhost:3000 и создайте свою первую ИИ-личность!

**Все работает локально, бесплатно и без цензуры! 🚀**
